import 'package:flutter/material.dart';

class TeamsView extends StatelessWidget {
  const TeamsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('صفحة إدارة الفرق (قيد التطوير)'),
    );
  }
}
